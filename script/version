#!/usr/bin/env bash

function version {
  local shome="${_packer_home:-"$(cd -P -- "$(dirname -- "$BASH_SOURCE")/.." && pwd -P)"}"
  source "$shome/script/profile"

  local ver_packer="$(packer --version 2>&- || true)"
  jq -n --arg packer "$ver_packer" '{packer: $packer}'
}

version "$@"
